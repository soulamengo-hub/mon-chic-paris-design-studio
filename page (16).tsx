'use client';import {useEffect,useState} from 'react';import {Nav} from '../../components/Nav';import type {Product,Expense} from '../../lib/types';import {eur,pct} from '../../lib/format';import {countBy,getStats} from '../../lib/analytics';
export default function BIPage(){const[products,setProducts]=useState<Product[]>([]);const[expenses,setExpenses]=useState<Expense[]>([]);useEffect(()=>{Promise.all([fetch('/api/products',{cache:'no-store'}),fetch('/api/expenses',{cache:'no-store'})]).then(async([a,b])=>{setProducts((await a.json()).products||[]);setExpenses((await b.json()).expenses||[])})},[]);const s=getStats(products,expenses);return <><Nav/><main className="mx-auto max-w-7xl p-6 space-y-5"><section className="card"><h1 className="text-3xl font-black text-monchic-green">Business Intelligence</h1><p>Interne Daten. Kunden sehen diese Werte nie.</p></section><section className="grid-dashboard"><div className="card"><div className="small-muted">Umsatz</div><div className="metric">{eur(s.revenue)}</div></div><div className="card"><div className="small-muted">Rohertrag</div><div className="metric">{eur(s.margin)}</div></div><div className="card"><div className="small-muted">Ausgaben</div><div className="metric">{eur(s.expenseTotal)}</div></div><div className="card"><div className="small-muted">Gewinn nach Ausgaben</div><div className="metric">{eur(s.netProfit)}</div></div><div className="card"><div className="small-muted">Retouren</div><div className="metric">{s.returns}</div></div><div className="card"><div className="small-muted">Retourenquote</div><div className="metric">{pct(s.returnRate)}</div></div></section><section className="grid md:grid-cols-4 gap-4"><List title="Saison" items={countBy(products,'season')}/><List title="Kanäle" items={countBy(products,'sales_channel')}/><List title="Kategorien" items={countBy(products,'category')}/><List title="Ausgaben" items={countBy(expenses,'category')}/></section></main></>}function List({title,items}:{title:string;items:[string,number][]}){return <div className="card"><h2 className="text-xl font-bold text-monchic-green">{title}</h2>{items.map(([n,c])=><div key={n} className="flex justify-between border-b py-2"><span>{n}</span><b>{c}</b></div>)}</div>}
